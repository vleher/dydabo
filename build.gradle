apply plugin: 'maven'

group = 'com.dydabo'
version = '0.1.0'

description = "Dynamic Database BlackBox"

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}

repositories {
    mavenLocal()
    mavenCentral()
}

uploadArchives {
    repositories {
        mavenLocal()
    }
}
